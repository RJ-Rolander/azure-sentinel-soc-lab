# Screenshots

Captures referenced by the README and docs. Redact tenant ID, subscription ID, workspace
ID, and any personal email before committing.

Expected files and what each should show:

| Filename | What it shows |
|---|---|
| `incident-queue.png` | Incidents queue with incidents across all four rule types |
| `architecture.png` | Rendered architecture diagram (optional, draw.io / Excalidraw) |
| `entity-mapping.png` | Alert enhancement > Entity mapping panel on one analytics rule |
| `correlation-incident.png` | Incident ID 13 graph, Resolved / True positive, account + host nodes |
| `incident-resolved.png` | A resolved, classified incident with the investigation comment visible |

Optional extras that strengthen the repo:

| Filename | What it shows |
|---|---|
| `kql-detection-results.png` | A detection query with results in Advanced hunting |
| `correlation-query.png` | The 4720+4732 SID join returning a matched row |
| `analytics-rules-list.png` | The Analytics page listing all four enabled rules |
| `mitre-tags.png` | An alert showing its mapped MITRE technique |
